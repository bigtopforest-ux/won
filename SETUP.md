# 4팀 종합관리 대시보드 - 설치 안내

이 폴더는 3팀에서 쓰던 관리 사이트 코드를 그대로 복사한 것입니다. 데이터는 전부 비워서 새로 시작할 수 있게 해놨습니다.

## 배포 방법 (Netlify)

1. [Netlify](https://app.netlify.com)에 가입/로그인
2. "Add new site" → "Deploy manually"를 선택하고, 이 `site` 폴더를 통째로 드래그해서 업로드
   - 또는 GitHub 저장소로 올린 뒤 Netlify에서 연결해도 됩니다
3. 배포가 끝나면 Netlify 대시보드에서 **Site settings → Environment variables**로 이동해서 아래 변수를 추가합니다:
   - `TEAM_KEY` = 팀 내부에서 쓸 비밀번호 (예: `changeme2026` 등 원하는 값으로)
4. `index.html` 안에서 같은 값으로 맞춰줘야 하는 부분이 있습니다:
   - `GATE_PASSWORD = 'changeme2026'` 부분을 3번에서 정한 `TEAM_KEY`와 **똑같은 값**으로 바꿔주세요 (`Ctrl+F`로 `changeme2026` 검색하면 한 곳 나옵니다)
5. 환경변수를 바꿨거나 index.html을 수정했으면 다시 배포(재배포)해야 반영됩니다

## 처음 접속했을 때

- 접속하면 비밀번호 입력 화면이 나오는데, 4번에서 설정한 비밀번호를 입력하면 들어갈 수 있습니다
- 모든 데이터(신규거래처, 사육현황, 출하관리, 약품비, 폭염관리 등)는 비어있는 상태로 시작합니다 — 각 탭의 "엑셀 업로드" 버튼으로 팀 자체 데이터를 올리면 됩니다

## 참고

- 사이트 이름/제목은 전부 "4팀"으로 바꿔놨는데, 원하는 이름으로 다시 바꾸고 싶으면 `index.html`에서 "4팀"을 찾아 바꾸면 됩니다
- 코드 구조나 각 기능이 어떻게 동작하는지 궁금하면 Claude Code한테 이 폴더를 열어서 물어보면 설명해줄 수 있습니다
